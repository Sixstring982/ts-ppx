{
  "name": "ts-ppx",
  "private": false,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.ts\"",
    "core:test": "cd packages/core && pnpm test",
    "example:start": "cd packages/example && pnpm start",
    "example:codegen": "cd packages/example && pnpm codegen"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.0.11",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
